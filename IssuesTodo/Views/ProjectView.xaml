<UserControl x:Class="IssuesTodo.Views.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:IssuesTodo.ViewModels">
    <DockPanel>

        <!-- Project header -->
        <Border DockPanel.Dock="Top" Padding="24,16,16,14">
            <DockPanel>
                <!-- Action buttons -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource MaterialDesignIconButton}"
                            Command="{Binding OpenInCodeCommand}"
                            ToolTip="Open folder in VS Code">
                        <materialDesign:PackIcon Kind="CodeBraces" Width="18" Height="18"/>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignIconButton}"
                            Command="{Binding ArchiveCommand}"
                            ToolTip="Archive project" Opacity="0.6">
                        <materialDesign:PackIcon Kind="ArchiveOutline" Width="18" Height="18"/>
                    </Button>
                </StackPanel>

                <!-- Project name + category -->
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Name}"
                               Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                    <TextBlock Text="{Binding Category}"
                               Style="{StaticResource MaterialDesignCaptionTextBlock}"
                               Opacity="0.5" Margin="0,2,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Add task input -->
        <Border DockPanel.Dock="Bottom" Padding="20,8,20,16">
            <DockPanel>
                <Button DockPanel.Dock="Right"
                        Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding AddTaskCommand}"
                        ToolTip="Add task" Margin="8,0,0,0">
                    <materialDesign:PackIcon Kind="Plus" Width="20" Height="20"/>
                </Button>
                <ComboBox DockPanel.Dock="Right" Width="100"
                          ItemsSource="{Binding PriorityOptions}"
                          SelectedItem="{Binding NewTaskPriority}"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          Margin="8,0,0,0"/>
                <TextBox materialDesign:HintAssist.Hint="Add a task..."
                         materialDesign:HintAssist.IsFloating="True"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding NewTaskText, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding AddTaskCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
            </DockPanel>
        </Border>

        <!-- Empty state when no open tasks -->
        <TextBlock DockPanel.Dock="Top"
                   Text="No open tasks"
                   Style="{StaticResource MaterialDesignBody1TextBlock}"
                   Foreground="{DynamicResource MaterialDesign.Brush.ForegroundLight}"
                   HorizontalAlignment="Center" Margin="0,40,0,0"
                   Visibility="{Binding HasVisibleTasks, Converter={StaticResource InvertBoolToVis}}"/>

        <!-- Task list -->
        <ScrollViewer Padding="20,4,20,20" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding OpenTasks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:TaskViewModel}">
                        <Border x:Name="TaskRow" Padding="12,8" Margin="0,2" CornerRadius="8" Background="Transparent">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#0DFFFFFF"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <DockPanel>
                                <!-- Priority stripe -->
                                <Rectangle DockPanel.Dock="Left" Width="3" Margin="0,0,12,0"
                                           RadiusX="1.5" RadiusY="1.5"
                                           Fill="{Binding Model.Priority, Converter={StaticResource PriorityToBrush}}"/>
                                <!-- Done checkbox -->
                                <CheckBox DockPanel.Dock="Right"
                                          IsChecked="{Binding IsDone}"
                                          VerticalAlignment="Center"
                                          Margin="12,0,0,0"/>
                                <!-- Edit toggle (hidden while editing) -->
                                <Button DockPanel.Dock="Right"
                                        Style="{StaticResource MaterialDesignIconButton}"
                                        Command="{Binding BeginEditCommand}"
                                        ToolTip="Edit task" Width="32" Height="32" Padding="6"
                                        Visibility="{Binding IsEditing, Converter={StaticResource InvertBoolToVis}}">
                                    <materialDesign:PackIcon Kind="PencilOutline" Width="16" Height="16"/>
                                </Button>

                                <Grid>
                                    <!-- Display mode: struck through once checked, removed from the file on exit -->
                                    <TextBlock Text="{Binding Model.Text}"
                                               VerticalAlignment="Center"
                                               TextWrapping="Wrap"
                                               Visibility="{Binding IsEditing, Converter={StaticResource InvertBoolToVis}}">
                                        <TextBlock.InputBindings>
                                            <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding BeginEditCommand}"/>
                                        </TextBlock.InputBindings>
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignBody1TextBlock}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDone}" Value="True">
                                                        <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                        <Setter Property="Opacity" Value="0.5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <!-- Edit mode -->
                                    <DockPanel Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
                                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="8,0,0,0">
                                            <ComboBox Width="100"
                                                      ItemsSource="{Binding PriorityOptions}"
                                                      SelectedItem="{Binding EditPriority}"
                                                      Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                    Command="{Binding CommitEditCommand}"
                                                    ToolTip="Save" Width="32" Height="32" Padding="6" Margin="4,0,0,0">
                                                <materialDesign:PackIcon Kind="Check" Width="18" Height="18"/>
                                            </Button>
                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                    Command="{Binding CancelEditCommand}"
                                                    ToolTip="Cancel" Width="32" Height="32" Padding="6">
                                                <materialDesign:PackIcon Kind="Close" Width="18" Height="18"/>
                                            </Button>
                                        </StackPanel>
                                        <TextBox Text="{Binding EditText, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                                 VerticalAlignment="Center">
                                            <TextBox.InputBindings>
                                                <KeyBinding Key="Return" Command="{Binding CommitEditCommand}"/>
                                                <KeyBinding Key="Escape" Command="{Binding CancelEditCommand}"/>
                                            </TextBox.InputBindings>
                                        </TextBox>
                                    </DockPanel>
                                </Grid>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
