<Window x:Class="IssuesTodo.Views.AddReminderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Add Reminder" Width="360" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Style="{StaticResource MaterialDesignWindow}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        Background="{DynamicResource MaterialDesign.Brush.Background}">
    <StackPanel Margin="24">
        <TextBlock Text="Add Reminder"
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Margin="0,0,0,20"/>

        <TextBox x:Name="TextBox"
                 materialDesign:HintAssist.Hint="What to remind you about"
                 materialDesign:HintAssist.IsFloating="True"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,16"/>

        <Grid Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>
            <DatePicker x:Name="DatePicker" Grid.Column="0"
                        materialDesign:HintAssist.Hint="Date"
                        materialDesign:HintAssist.IsFloating="True"
                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
            <TextBox x:Name="TimeBox" Grid.Column="2"
                     materialDesign:HintAssist.Hint="HH:mm"
                     materialDesign:HintAssist.IsFloating="True"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
        </Grid>

        <TextBlock x:Name="ErrorText" Foreground="#EF5350" FontSize="12"
                   Margin="0,-16,0,12" Visibility="Collapsed"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="CANCEL"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="Cancel_Click" Margin="0,0,8,0"/>
            <Button Content="ADD"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="Add_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
